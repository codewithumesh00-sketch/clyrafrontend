// import { AIMode, PromptIntent } from "@/types/ai";
// import { analyzePrompt } from "./promptAnalyzer";

// export function resolveIntent(
//   prompt: string,
//   selectedMode: AIMode
// ): PromptIntent {
//   if (selectedMode === "chat") return "conversation";
//   if (selectedMode === "generate") return "website_generation";
//   if (selectedMode === "edit") return "website_edit";
//   if (selectedMode === "quick_fix") return "quick_fix";

//   return analyzePrompt(prompt);
// }